[workspace]
members = ["steel-host", "steel-plugin-cli"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"
authors = ["Alvsch"]

[profile.release]
lto = true
codegen-units = 1
panic = "abort"

[profile.profiling]
inherits = "release"
debug = true

[workspace.dependencies]
# Steel
steel-utils = { git = "https://github.com/Alvsch/SteelMC", branch = "plugin-api" }

# Core runtime
tokio = "1.52.3"
mlua = { version = "0.12.0", features = ["luau-jit", "async", "send", "macros"] }
clap = { version = "4.6.4", features = ["derive"] }

# Error and config handling
anyhow = "1.0.103"
thiserror = "2.0.19"
serde = { version = "1.0.229", features = ["derive"] }
rmp-serde = "1.3.1"
toml = "1.1.3"
semver = "1.0.28"
slotmap = "1.1.1"
heed = "0.22.1"

# File and archive handling
tempfile = "3.27.0"
walkdir = "2.5.0"
tar = "0.4.46"
flate2 = "1.1.9"
zstd = "0.13.3"
zip = "8.6.0"

# Logging
tracing = "0.1.44"
tracing-subscriber = "0.3.23"

[workspace.lints.rust]
# TODO: change to warn
missing_docs = "allow"

[workspace.lints.clippy]
single_call_fn = "allow"
multiple_inherent_impl = "allow"
shadow_unrelated = "allow"
missing_errors_doc = "allow"
struct_excessive_bools = "allow"
needless_pass_by_value = "allow"
cargo_common_metadata = "allow"
cast_sign_loss = "allow"
cast_possible_wrap = "allow"
cast_possible_truncation = "allow"
cast_precision_loss = "allow"
absolute_paths = "warn"
missing_const_for_fn = "warn"
# TOOD: remove
missing_panics_doc = "allow"

all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -2 }
unwrap_used = { level = "warn", priority = -3 }
